{% extends 'base.html.twig' %}

{% block title %}PointDeVente index{% endblock %}

{% block body %}
    <h1>PointDeVente index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Partenaire</th>
                <th>Produits</th>
                <th>Quantity_month</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for point_de_vente in point_de_ventes %}
            <tr>
                <td>{{ point_de_vente.id }}</td>
                <td>{{ point_de_vente.Partenaire }}</td>
                <td>{{ point_de_vente.Produits }}</td>
                <td>{{ point_de_vente.QuantityMonth }}</td>
                <td>
                    <a href="{{ path('point_de_vente_show', {'id': point_de_vente.id}) }}">show</a>
                    <a href="{{ path('point_de_vente_edit', {'id': point_de_vente.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('point_de_vente_new') }}">Create new</a>
{% endblock %}
